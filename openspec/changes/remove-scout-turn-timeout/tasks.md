# Tasks

- [ ] Remove implicit and public launcher turn deadlines across SDK, web, and client socket.
- [ ] Make the supervision watcher indefinite by default while preserving its optional observer-only timeout.
- [ ] Update the canonical specification, skill, CLI usage and README.
- [ ] Verify simulated long turn, disconnect, finite observer timeout, focused regression suite and installed package.
